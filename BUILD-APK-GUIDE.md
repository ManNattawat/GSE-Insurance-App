# คู่มือ Build APK ผ่าน Git (GitHub Actions)

## 📋 ขั้นตอนการ Build APK

### 1. สร้าง Expo Access Token

1. ไปที่ [Expo Dashboard](https://expo.dev)
2. Login เข้าสู่ระบบ
3. ไปที่ **Account Settings** > **Access Tokens**
   - หรือไปที่: `https://expo.dev/accounts/[your-username]/settings/access-tokens`
4. คลิก **Create Token**
5. ตั้งชื่อ Token (เช่น: `github-actions`)
6. Copy Token ที่สร้างมา (จะแสดงแค่ครั้งเดียว!)

---

### 2. ตั้งค่า GitHub Secrets

1. ไปที่ GitHub Repository ของคุณ
2. ไปที่ **Settings** > **Secrets and variables** > **Actions**
3. คลิก **New repository secret**
4. ตั้งค่า:
   - **Name**: `EXPO_TOKEN`
   - **Value**: วาง Expo Token ที่ copy มา
5. คลิก **Add secret**

---

### 3. Push Code ไปยัง GitHub

```bash
# ตรวจสอบสถานะ
git status

# เพิ่มไฟล์ทั้งหมด
git add .

# Commit
git commit -m "Ready for APK build"

# Push ไปยัง main branch
git push origin main
```

**หรือถ้าใช้ branch อื่น:**
```bash
git push origin master
```

---

### 4. ตรวจสอบ Build Status

#### ใน GitHub:
1. ไปที่ **Actions** tab ใน repository
2. จะเห็น workflow "Build APK" กำลังรัน
3. คลิกเพื่อดูรายละเอียด

#### ใน Expo Dashboard:
1. ไปที่ [Expo Dashboard](https://expo.dev)
2. ไปที่ **Builds** section
3. จะเห็น build กำลังรัน

---

### 5. Download APK

1. รอ build เสร็จ (ประมาณ 10-20 นาที)
2. ไปที่ Expo Dashboard > **Builds**
3. คลิก build ที่เสร็จแล้ว
4. คลิก **Download** เพื่อดาวน์โหลด APK

---

## 🚀 วิธี Build แบบ Manual (ไม่ต้อง Push)

### ใช้ GitHub Actions Manual Trigger:

1. ไปที่ **Actions** tab
2. เลือก workflow **"Build APK"**
3. คลิก **Run workflow**
4. เลือก branch (main/master)
5. คลิก **Run workflow**

---

## ⚙️ Build Configuration

### Profile ที่ใช้:
- **preview**: Build APK สำหรับทดสอบ
- **production**: Build APK สำหรับ production

### ตั้งค่าใน `eas.json`:
```json
{
  "build": {
    "preview": {
      "android": {
        "buildType": "apk"
      }
    }
  }
}
```

---

## 📱 ติดตั้ง APK บน Tablet

1. **Transfer APK ไปยัง Tablet:**
   - Email APK ไปยังตัวเอง
   - หรือใช้ USB transfer
   - หรือใช้ Google Drive/Dropbox

2. **เปิดการติดตั้งจากแหล่งที่ไม่รู้จัก:**
   - Settings > Security > Unknown Sources (เปิด)

3. **ติดตั้ง APK:**
   - เปิดไฟล์ APK
   - คลิก Install
   - รอให้ติดตั้งเสร็จ

4. **เปิดแอป:**
   - เปิดแอป GSE Insurance App
   - ทดสอบฟีเจอร์ต่างๆ

---

## 🔍 ตรวจสอบ Build

### Build จะเริ่มเมื่อ:
- ✅ Push ไปยัง `main` หรือ `master` branch
- ✅ สร้าง tag ที่ขึ้นต้นด้วย `v` (เช่น `v1.0.0`)
- ✅ Manual trigger จาก GitHub Actions

### Build จะใช้เวลา:
- ⏱️ ประมาณ 10-20 นาที
- ขึ้นอยู่กับขนาดแอปและ dependencies

---

## ❌ แก้ไขปัญหา

### Build ล้มเหลว:
1. ตรวจสอบว่า `EXPO_TOKEN` ถูกตั้งค่าใน GitHub Secrets
2. ตรวจสอบว่า Token ยังใช้งานได้ (ไม่หมดอายุ)
3. ตรวจสอบ error logs ใน GitHub Actions

### APK ไม่ติดตั้ง:
1. ตรวจสอบว่าเปิด "Unknown Sources" แล้ว
2. ตรวจสอบว่า APK ไม่เสียหาย (download ใหม่)
3. ตรวจสอบ Android version (ต้อง Android 5.0+)

---

## 📝 Checklist

- [ ] สร้าง Expo Access Token
- [ ] ตั้งค่า `EXPO_TOKEN` ใน GitHub Secrets
- [ ] Push code ไปยัง GitHub
- [ ] ตรวจสอบ Build Status
- [ ] Download APK
- [ ] ติดตั้งบน Tablet
- [ ] ทดสอบแอป

---

## ✅ สรุป

1. **สร้าง Token** → Expo Dashboard
2. **ตั้งค่า Secret** → GitHub Settings
3. **Push Code** → `git push origin main`
4. **รอ Build** → 10-20 นาที
5. **Download APK** → Expo Dashboard
6. **ติดตั้ง** → บน Tablet

**พร้อม Build แล้ว!** 🚀

