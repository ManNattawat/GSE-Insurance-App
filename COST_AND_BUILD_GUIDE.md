# 💰 ค่าใช้จ่ายและวิธี Build ที่ประหยัด

## 📊 ค่าใช้จ่าย

### 1. **GitHub Actions** (Free Tier)

**สำหรับ Public Repository:**
- ✅ **Unlimited** minutes
- ✅ **Unlimited** builds
- ✅ **ฟรี 100%**

**สำหรับ Private Repository:**
- ⚠️ **2,000 minutes/เดือน** (ฟรี)
- ⚠️ **500 MB storage** (ฟรี)
- 💰 **เกินแล้ว**: $0.008/minute

**Build Time ต่อครั้ง:**
- React Native CLI: ~8-15 นาที
- EAS Build: ~10-15 นาที (แต่ build บน cloud ไม่ใช้ minutes)

---

### 2. **EAS Build** (Expo)

**Free Tier:**
- ✅ **30 builds/เดือน** (ฟรี)
- ✅ **Build บน cloud** (ไม่ใช้เครื่อง)
- ⚠️ **Queue อาจยาว** (รอได้)

**Paid Plans:**
- 💰 **Production Plan**: $29/เดือน (unlimited builds)
- 💰 **Enterprise**: Custom pricing

---

## 🎯 วิธี Build ที่ประหยัด

### ✅ **แนะนำ: ใช้ GitHub Actions (Public Repo)**

**ข้อดี:**
- ฟรี 100% (unlimited)
- Build อัตโนมัติ
- ไม่ต้องใช้เครื่อง

**วิธีทำ:**
1. ตั้งค่า repo เป็น **Public** (ถ้ายังไม่ใช่)
2. ใช้ GitHub Actions workflow
3. Build อัตโนมัติทุกครั้งที่ push

---

### ⚠️ **ถ้าใช้ Private Repo**

**วิธีประหยัด:**
1. **Build เฉพาะเมื่อจำเป็น:**
   - ไม่ build ทุก commit
   - Build เฉพาะ tag/release
   - ใช้ `[skip ci]` ใน commit message

2. **ใช้ EAS Build แทน:**
   - Build บน cloud (ไม่ใช้ GitHub minutes)
   - แต่มี limit 30 builds/เดือน

3. **Local Build (ถ้าเครื่องแรงพอ):**
   - ใช้ `npx react-native run-android`
   - แต่เครื่องคุณสเปกต่ำ → **ไม่แนะนำ**

---

## 🚀 วิธี Build ที่แนะนำ

### **Option 1: GitHub Actions (Public Repo)** ⭐ **แนะนำ**

```yaml
# .github/workflows/build-apk.yml
# Build อัตโนมัติทุกครั้งที่ push
```

**ข้อดี:**
- ฟรี 100%
- ไม่จำกัดจำนวน build
- Build อัตโนมัติ

---

### **Option 2: EAS Build**

```bash
# Build ผ่าน EAS (30 builds/เดือน ฟรี)
npx eas-cli build --platform android --profile preview
```

**ข้อดี:**
- Build บน cloud (ไม่ใช้เครื่อง)
- ไม่ใช้ GitHub minutes

**ข้อเสีย:**
- จำกัด 30 builds/เดือน
- Queue อาจยาว

---

### **Option 3: Hybrid (ประหยัดที่สุด)**

**สำหรับ Development:**
- ใช้ **Expo Go** หรือ **Development Build** (ไม่ต้อง build APK)

**สำหรับ Testing:**
- Build ผ่าน **GitHub Actions** (Public repo = ฟรี)

**สำหรับ Production:**
- Build ผ่าน **EAS Build** (Production profile)

---

## 📝 คำแนะนำ

### ✅ **ทำได้:**
1. Build บ่อยๆ (ถ้า Public repo)
2. ใช้ GitHub Actions สำหรับ testing
3. ใช้ EAS Build สำหรับ production

### ⚠️ **ระวัง:**
1. **Private repo** → จำกัด 2,000 minutes/เดือน
2. **EAS Build** → จำกัด 30 builds/เดือน
3. **Build บ่อยเกินไป** → เสียเวลา + quota

### 💡 **Best Practice:**
1. **Development**: ใช้ Expo Go หรือ Development Build
2. **Testing**: Build เฉพาะเมื่อจำเป็น
3. **Production**: Build เฉพาะ release/tag

---

## 🔧 แก้ไข Error "Unable to load script"

**สาเหตุ:**
- APK ที่ build มาผิดพลาด
- Component name ไม่ตรงกัน

**แก้ไขแล้ว:**
- ✅ แก้ `MainActivity.kt` ให้ใช้ `"main"` แทน `"GSE-Insurance-App"`

**ขั้นตอนต่อไป:**
1. Build APK ใหม่
2. ติดตั้ง APK ใหม่
3. ทดสอบ

---

## 📊 สรุปค่าใช้จ่าย

| Service | Free Tier | เกินแล้ว |
|---------|-----------|----------|
| **GitHub Actions (Public)** | ✅ Unlimited | - |
| **GitHub Actions (Private)** | ⚠️ 2,000 min/เดือน | $0.008/min |
| **EAS Build** | ⚠️ 30 builds/เดือน | $29/เดือน |

**แนะนำ: ใช้ Public Repo + GitHub Actions = ฟรี 100%** 🎉

