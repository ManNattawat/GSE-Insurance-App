# อธิบาย Dependencies ที่ Expo Go ไม่รองรับ

## 📦 Dependencies ที่ต้องรู้

### 1. **@react-navigation/native** + **@react-navigation/bottom-tabs** + **@react-navigation/native-stack**

#### ทำอะไร?
```
ระบบนำทาง (Navigation) สำหรับ React Native
```

**หน้าที่:**
- ✅ เปลี่ยนหน้าจอ (Screen transitions)
- ✅ Bottom Tabs (แท็บด้านล่าง)
- ✅ Stack Navigation (ย้อนกลับได้)
- ✅ จัดการ history

**ตัวอย่าง:**
```
หน้าหลัก → หน้าลูกค้า → เพิ่มลูกค้า → (ย้อนกลับ)
```

**ทำไม Expo Go ไม่รองรับ?**
- ใช้ Native Code (C++/Java)
- ต้อง compile เข้าไปในแอป
- Expo Go เป็นแอปสำเร็จรูป ไม่มีโค้ดนี้

**ทางเลือก:**
- ✅ Development Build (รองรับ)
- ✅ APK (รองรับ)
- ❌ Expo Go (ไม่รองรับ)

---

### 2. **xlsx**

#### ทำอะไร?
```
อ่าน/เขียนไฟล์ Excel (.xlsx)
```

**หน้าที่:**
- ✅ สร้างไฟล์ Excel
- ✅ อ่านไฟล์ Excel
- ✅ แปลงข้อมูลเป็น Excel format

**ตัวอย่างในแอป:**
```
ข้อมูลลูกค้า → Export → customers.xlsx
```

**ทำไม Expo Go ไม่รองรับ?**
- ใช้ Native Modules
- ต้องเข้าถึง File System
- Expo Go มีข้อจำกัดเรื่อง File System

**ทางเลือก:**
- ✅ Development Build (รองรับ)
- ✅ APK (รองรับ)
- ⚠️ Web Browser (รองรับบางส่วน)
- ❌ Expo Go (ไม่รองรับ)

---

## 🔍 เปรียบเทียบ

| Dependency | หน้าที่ | ทำไม Expo Go ไม่รองรับ | ทางเลือก |
|-----------|--------|---------------------|---------|
| **@react-navigation** | ระบบนำทาง | ใช้ Native Code | Development Build / APK |
| **xlsx** | Export Excel | ใช้ Native Modules | Development Build / APK |

---

## 💡 สรุปสั้นๆ

### @react-navigation
```
"ระบบนำทาง" = เปลี่ยนหน้าจอ, แท็บ, ย้อนกลับ
❌ Expo Go ไม่มีโค้ดนี้
✅ ต้อง Build APK หรือ Development Build
```

### xlsx
```
"Export Excel" = ส่งออกข้อมูลเป็นไฟล์ Excel
❌ Expo Go ไม่รองรับ File System
✅ ต้อง Build APK หรือ Development Build
```

---

## 🎯 วิธีแก้

### ถ้าต้องการใช้ Expo Go:
1. **ลบ Navigation:**
   - ใช้ Expo Router แทน (ต้องเขียนโค้ดใหม่)

2. **ลบ xlsx:**
   - ใช้ CSV แทน (ง่ายกว่า แต่ไม่ใช่ Excel)
   - หรือใช้ Web API ส่งข้อมูล

**⚠️ ไม่แนะนำ:** ต้องเขียนโค้ดใหม่เยอะมาก

### แนะนำ:
**Build APK หรือ Development Build** - รองรับทุกอย่าง

---

## 📝 ตัวอย่างการใช้งาน

### Navigation:
```typescript
// เปลี่ยนหน้าจอ
navigation.navigate('CustomerForm');

// แท็บด้านล่าง
<Tab.Navigator>
  <Tab.Screen name="Customers" />
</Tab.Navigator>
```

### xlsx:
```typescript
// Export Excel
await excelService.exportToExcel(customers);
// → สร้างไฟล์ customers.xlsx
```

---

## ✅ สรุป

| Dependency | คืออะไร | ใช้ทำอะไร | Expo Go |
|-----------|---------|----------|---------|
| **@react-navigation** | ระบบนำทาง | เปลี่ยนหน้าจอ | ❌ |
| **xlsx** | Excel Library | Export Excel | ❌ |

**คำแนะนำ:** ใช้ Development Build หรือ Build APK แทน Expo Go

