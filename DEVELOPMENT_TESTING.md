# 🧪 คู่มือการทดสอบบนแท็บเล็ตผ่าน USB

**วิธีทดสอบการแก้ไขโค้ดบนแท็บเล็ต Samsung โดยไม่ต้อง build APK ทุกครั้ง**

---

## 🎯 หลักการทำงาน

1. **Build Debug APK ครั้งเดียว** (ใช้เวลานาน ~5-10 นาที)
2. **ติดตั้ง Debug APK บนแท็บเล็ต**
3. **รัน Metro Bundler** (ใช้เวลา ~30 วินาที)
4. **แก้ไขโค้ด → Hot Reload อัตโนมัติ** (ไม่ต้อง build APK ใหม่!)

---

## 📋 ขั้นตอนการตั้งค่า (ทำครั้งเดียว)

### 1. เปิด USB Debugging บนแท็บเล็ต Samsung

1. ไปที่ **Settings** → **About tablet**
2. กด **Build number** 7 ครั้ง (จะเห็น "You are now a developer!")
3. กลับไป **Settings** → **Developer options**
4. เปิด **USB debugging**
5. เชื่อมต่อแท็บเล็ตกับคอมพิวเตอร์ผ่าน USB

### 2. ตรวจสอบการเชื่อมต่อ

```bash
# ตรวจสอบว่าเห็นแท็บเล็ตหรือไม่
adb devices
```

**ผลลัพธ์ที่ถูกต้อง:**
```
List of devices attached
ABC123XYZ    device
```

**ถ้าเห็น "unauthorized":**
- ดูที่หน้าจอแท็บเล็ต → จะมี popup "Allow USB debugging?"
- กด **Allow** หรือ **OK**

---

## 🚀 วิธีใช้งาน (ทุกครั้งที่ต้องการทดสอบ)

### **วิธีที่ 1: Build และติดตั้ง Debug APK (ครั้งแรก หรือเมื่อเปลี่ยน Native Code)**

```bash
# 1. เริ่ม Metro Bundler (Terminal 1)
npm start

# 2. Build และติดตั้ง Debug APK บนแท็บเล็ต (Terminal 2)
npm run android
```

**คำอธิบาย:**
- `npm start` → เริ่ม Metro bundler (JavaScript bundler)
- `npm run android` → Build Debug APK + ติดตั้งบนแท็บเล็ตอัตโนมัติ
- ใช้เวลา: **~5-10 นาที** (ครั้งแรก) หรือ **~2-3 นาที** (ครั้งถัดไป)

**หลังจากนี้:**
- แอพจะเปิดบนแท็บเล็ตอัตโนมัติ
- แก้ไขโค้ด JavaScript/TypeScript → **Hot Reload อัตโนมัติ!**
- **ไม่ต้อง build APK ใหม่!**

---

### **วิธีที่ 2: ใช้แอพที่ติดตั้งไว้แล้ว (แนะนำ - เร็วกว่า!)**

ถ้าติดตั้ง Debug APK ไว้แล้ว แค่รัน Metro Bundler:

```bash
# Terminal 1: เริ่ม Metro Bundler
npm start
```

**จากนั้น:**
1. เปิดแอพบนแท็บเล็ต (ถ้ายังไม่เปิด)
2. กด **Shake gesture** (เขย่าแท็บเล็ต) หรือกด **Menu** → **Reload**
3. หรือแก้ไขโค้ด → **Fast Refresh จะทำงานอัตโนมัติ!**

---

## ⚡ Hot Reload / Fast Refresh

### **ทำงานอัตโนมัติ:**
- แก้ไขโค้ด JavaScript/TypeScript → แอพจะ reload อัตโนมัติ
- ใช้เวลา: **~1-3 วินาที**

### **ถ้าไม่ reload อัตโนมัติ:**
1. **Shake gesture** บนแท็บเล็ต → เลือก **Reload**
2. หรือกด **R+R** (กด R สองครั้ง) ใน Metro Bundler terminal
3. หรือกด **Ctrl+M** (Windows) / **Cmd+M** (Mac) → เลือก **Reload**

---

## 🔧 คำสั่งที่มีประโยชน์

### **ตรวจสอบการเชื่อมต่อ:**
```bash
adb devices
```

### **ดู Logs จากแท็บเล็ต:**
```bash
# ดู logs แบบ real-time
npx react-native log-android

# หรือใช้ adb
adb logcat | grep ReactNativeJS
```

### **Reload แอพด้วยคำสั่ง:**
```bash
# ส่งคำสั่ง reload ไปยังแอพ
adb shell input keyevent 82  # เปิด Dev Menu
# แล้วเลือก Reload
```

### **Uninstall แอพ (ถ้าต้องการเริ่มใหม่):**
```bash
adb uninstall com.gse.insurance
```

### **ติดตั้ง APK ที่ build ไว้แล้ว:**
```bash
# ถ้ามี APK อยู่แล้ว
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

---

## 🐛 Debug Mode

### **เปิด Developer Menu:**
- **Shake gesture** บนแท็บเล็ต
- หรือกด **Ctrl+M** (Windows) / **Cmd+M** (Mac)

### **ตัวเลือกใน Developer Menu:**
- **Reload** → Reload แอพ
- **Debug** → เปิด Chrome DevTools
- **Show Inspector** → ดู UI Inspector
- **Enable Hot Reloading** → เปิด/ปิด Hot Reload
- **Enable Fast Refresh** → เปิด/ปิด Fast Refresh

---

## ⚠️ เมื่อไหร่ที่ต้อง Build APK ใหม่?

### **ต้อง Build ใหม่:**
- ✅ เปลี่ยน **Native Code** (Java/Kotlin ใน `android/`)
- ✅ เพิ่ม/ลบ **Native Dependencies** (เช่น react-native-vector-icons)
- ✅ เปลี่ยน **AndroidManifest.xml**
- ✅ เปลี่ยน **build.gradle** configuration
- ✅ เปลี่ยน **App Icon** หรือ **Splash Screen**

### **ไม่ต้อง Build ใหม่:**
- ✅ แก้ไข **JavaScript/TypeScript** (`.tsx`, `.ts`, `.js`)
- ✅ แก้ไข **Styles** (`.tsx` files)
- ✅ เปลี่ยน **Logic** ใน components
- ✅ แก้ไข **Navigation**
- ✅ แก้ไข **State Management**

---

## 📊 เปรียบเทียบเวลา

| วิธี | เวลา | เมื่อไหร่ใช้ |
|------|------|-------------|
| **Build APK** | 5-10 นาที | ครั้งแรก, เปลี่ยน Native Code |
| **Metro Bundler + Hot Reload** | 30 วินาที | ทุกครั้งที่แก้ไข JS/TS |
| **Fast Refresh** | 1-3 วินาที | แก้ไขโค้ด → Reload อัตโนมัติ |

---

## 🎯 Workflow ที่แนะนำ

### **ครั้งแรก:**
```bash
# 1. เชื่อมต่อแท็บเล็ตผ่าน USB
# 2. ตรวจสอบการเชื่อมต่อ
adb devices

# 3. Build และติดตั้ง Debug APK
npm run android
```

### **ทุกครั้งที่แก้ไขโค้ด:**
```bash
# 1. เริ่ม Metro Bundler (ถ้ายังไม่รัน)
npm start

# 2. แก้ไขโค้ด → Hot Reload อัตโนมัติ!
# 3. ดูผลลัพธ์บนแท็บเล็ตทันที
```

### **เมื่อแก้ไขเสร็จแล้ว:**
```bash
# Build Release APK สำหรับใช้งานจริง
cd android
./gradlew assembleRelease
```

---

## 🆘 แก้ไขปัญหา

### **ปัญหา: `adb devices` ไม่เห็นแท็บเล็ต**
```bash
# 1. ตรวจสอบ USB cable
# 2. ตรวจสอบว่าเปิด USB debugging แล้ว
# 3. ลอง unplug และ plug USB ใหม่
# 4. ตรวจสอบ USB drivers (Windows)
```

### **ปัญหา: Build ล้มเหลว**
```bash
# Clean build
cd android
./gradlew clean
cd ..
npm run android
```

### **ปัญหา: Hot Reload ไม่ทำงาน**
```bash
# 1. ตรวจสอบว่า Metro Bundler ยังรันอยู่
# 2. Shake gesture → Enable Hot Reloading
# 3. Reload แอพ (R+R)
```

### **ปัญหา: แอพ crash หรือ error**
```bash
# ดู logs
npx react-native log-android

# หรือ
adb logcat | grep ReactNativeJS
```

---

## 📝 สรุป

1. **Build Debug APK ครั้งเดียว** → `npm run android`
2. **รัน Metro Bundler** → `npm start`
3. **แก้ไขโค้ด** → **Hot Reload อัตโนมัติ!**
4. **ไม่ต้อง build APK ใหม่** เว้นแต่จะเปลี่ยน Native Code

**ประหยัดเวลา: จาก 5-10 นาที → เหลือ 1-3 วินาที!** ⚡

