# 🚀 Expo + GitHub Actions Build APK - คู่มือฉบับสมบูรณ์

## ✅ คำตอบ: **ได้แน่นอน!**

Expo รองรับการ build APK ผ่าน GitHub Actions ได้ 2 วิธี:

---

## 🎯 วิธีที่ 1: ใช้ EAS Build (แนะนำ)

### ข้อดี:
- ✅ Build บน cloud (ไม่ต้องใช้เครื่อง)
- ✅ เสถียรมาก (ไม่มีปัญหา build)
- ✅ เร็วกว่า local build
- ✅ รองรับ GitHub Actions ดี

### ข้อจำกัด:
- ⚠️ Free tier จำกัด (30 builds/เดือน)
- ⚠️ ต้องมี Expo account

### ขั้นตอน Setup:

#### 1. สร้าง Expo Account
```bash
npm install -g eas-cli
eas login
```

#### 2. สร้าง Expo Token
1. ไปที่: https://expo.dev/accounts/[username]/settings/access-tokens
2. สร้าง token ใหม่
3. เก็บไว้ใน GitHub Secrets (ชื่อ: `EXPO_TOKEN`)

#### 3. Setup EAS Build
```bash
eas build:configure
```

#### 4. สร้าง `eas.json`
```json
{
  "build": {
    "preview": {
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "android": {
        "buildType": "apk"
      }
    }
  }
}
```

#### 5. เพิ่ม GitHub Secrets
- ไปที่: GitHub Repo → Settings → Secrets and variables → Actions
- เพิ่ม secret: `EXPO_TOKEN` = token ที่สร้างไว้

#### 6. ใช้ Workflow ที่สร้างไว้
- ไฟล์: `.github/workflows/build-expo-apk.yml`
- Build อัตโนมัติเมื่อ push code

---

## 🎯 วิธีที่ 2: Build Local (ไม่ใช้ EAS)

### ข้อดี:
- ✅ ฟรี 100% (ไม่จำกัด)
- ✅ ไม่ต้องมี Expo account
- ✅ ควบคุมได้เต็มที่

### ข้อจำกัด:
- ⚠️ ต้อง config เอง
- ⚠️ อาจมีปัญหา build (เหมือนตอนนี้)

### ใช้ Project: `expo-react-native-cicd`

- GitHub: https://github.com/TanayK07/expo-react-native-cicd
- Build APK/AAB โดยไม่ใช้ EAS
- ฟรี 100%

---

## 📊 เปรียบเทียบ

| คุณสมบัติ | EAS Build | Local Build |
|---------|-----------|-------------|
| **ความเร็ว** | ⭐⭐⭐⭐ เร็ว | ⭐⭐⭐ ช้ากว่า |
| **เสถียร** | ⭐⭐⭐⭐⭐ ดีมาก | ⭐⭐⭐ ขึ้นกับเครื่อง |
| **ค่าใช้จ่าย** | ⭐⭐⭐ Free tier จำกัด | ⭐⭐⭐⭐⭐ ฟรี |
| **Setup** | ⭐⭐⭐⭐ ง่าย | ⭐⭐⭐ ต้อง config |
| **GitHub Actions** | ⭐⭐⭐⭐⭐ รองรับดี | ⭐⭐⭐⭐ รองรับ |

---

## 💡 คำแนะนำ

### สำหรับแอพประกันภัย:

**แนะนำ: ใช้ EAS Build** เพราะ:
- ✅ Build บน cloud (ไม่ต้องใช้เครื่อง)
- ✅ เสถียรกว่า (ไม่มีปัญหา build ที่เจอ)
- ✅ เร็วกว่า
- ✅ รองรับ GitHub Actions ดี

**ถ้า build เยอะมาก:**
- อาจต้อง upgrade เป็น paid plan
- หรือใช้ Local Build แทน

---

## 🚀 ขั้นตอนต่อไป

### ถ้าต้องการใช้ Expo:

1. **ย้ายไปใช้ Expo:**
   ```bash
   npx create-expo-app@latest
   ```

2. **Setup EAS Build:**
   ```bash
   eas build:configure
   ```

3. **เพิ่ม GitHub Secrets:**
   - `EXPO_TOKEN`

4. **ใช้ Workflow:**
   - `.github/workflows/build-expo-apk.yml`

---

## ✅ สรุป

**ได้!** Expo + GitHub Actions Build APK ได้แน่นอน

**วิธี:**
1. ใช้ **EAS Build** (แนะนำ) - Build บน cloud
2. หรือใช้ **Local Build** - Build ใน GitHub Actions

**ข้อดี:**
- Build บน cloud (ไม่ต้องใช้เครื่อง)
- เสถียรกว่า local build
- เร็วกว่า
- รองรับ GitHub Actions ดี

---

**สรุป: ใช้ Expo แล้ว build APK ผ่าน GitHub Actions ได้แน่นอน!** 🎉

