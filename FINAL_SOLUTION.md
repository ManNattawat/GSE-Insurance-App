# 🎯 วิธีแก้ไขสุดท้าย - Release APK ไม่มี Dev Menu

## ⚠️ ปัญหา
- APK ที่ติดตั้งเป็น **Release APK** (ไม่มี Dev Menu)
- เขย่าแท็บเล็ตแล้วไม่เกิดอะไร
- แอพไม่สามารถเชื่อมต่อกับ Metro Bundler ได้

## 🔍 สาเหตุ
**Release APK ไม่มี bundle รวมอยู่** และต้องเชื่อมต่อกับ Metro Bundler แต่:
- ไม่มี Dev Menu (ตั้งค่า Debug Server Host ไม่ได้)
- APK ที่ build อาจยังไม่มี code ที่แก้ไข MainApplication.kt

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: รอ Build APK ใหม่จาก GitHub Actions (แนะนำ)

**APK ที่ build อาจยังไม่มี code ที่แก้ไข MainApplication.kt**

1. **ตรวจสอบ GitHub Actions:**
   - ไปที่: `https://github.com/ManNattawat/GSE-Insurance-App/actions`
   - ดู workflow ล่าสุด → ต้อง build **หลังจาก commit `c4f7437`**
   
2. **ถ้ายัง build เก่า:**
   - รอให้ build ใหม่เสร็จ (ประมาณ 5-10 นาที)
   - ดาวน์โหลด APK ใหม่
   - ติดตั้งใหม่

3. **APK ใหม่ควรมี:**
   - Code ที่แก้ไข MainApplication.kt (เปิด Developer Support)
   - สามารถเชื่อมต่อกับ Metro Bundler ได้

---

### วิธีที่ 2: ใช้ Debug APK แทน

**ถ้า GitHub Actions build Debug APK สำเร็จ:**

1. ดาวน์โหลด **`gse-insurance-app-debug.apk`**
2. ติดตั้งบนแท็บเล็ต (แทน Release APK)
3. Debug APK จะมี Dev Menu (เขย่าแล้วมีเมนู)

---

### วิธีที่ 3: Build APK เอง (ถ้าจำเป็น)

**ถ้า GitHub Actions build ไม่สำเร็จ:**

```bash
# ต้องแก้ไขปัญหา build ก่อน
# หรือใช้วิธีอื่น
```

---

## 📊 สถานะ

- ✅ **Code:** แก้ไขแล้ว (MainApplication.kt - เปิด Developer Support)
- ✅ **Metro Bundler:** กำลังรันอยู่
- ⚠️ **APK:** อาจเป็นตัวเก่าหรือ Release

---

## 🎯 ขั้นตอนต่อไป

1. **ตรวจสอบ GitHub Actions:**
   - ดูว่า workflow build เสร็จแล้วหรือยัง
   - ดูว่า build หลังจาก commit `c4f7437` หรือไม่

2. **ถ้า build เสร็จแล้ว:**
   - ดาวน์โหลด APK ใหม่
   - ติดตั้งใหม่

3. **ถ้ายัง build ไม่เสร็จ:**
   - รอให้ build เสร็จ
   - หรือลองใช้ Debug APK (ถ้ามี)

---

**ไปตรวจสอบ GitHub Actions ก่อน → ดูว่า build เสร็จและมี APK ใหม่หรือยัง** 🚀

