# 🔧 แก้ไขการเชื่อมต่อ Metro Bundler - APK ใหม่

## ⚠️ ปัญหา
APK ใหม่ที่ติดตั้งแล้ว แต่ยังไม่สามารถเชื่อมต่อกับ Metro Bundler ได้

## 🔍 สาเหตุที่เป็นไปได้

1. **APK ที่ build จาก GitHub Actions อาจเป็น Release APK** (ไม่มี bundle รวมอยู่)
2. **APK อาจ build ก่อนการแก้ไข MainApplication.kt** (ยังไม่มี Developer Support)
3. **แอพไม่รู้ว่าจะเชื่อมต่อที่ไหน** (ต้องตั้งค่า Debug Server Host)

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: ตั้งค่า Debug Server Host (ถ้าเป็น Debug APK)

1. **เขย่าแท็บเล็ต** → Dev Menu ควรเปิดขึ้นมา
2. กด **"Settings"** หรือ **"Debug server host & port for device"**
3. ใส่:
   - **WiFi:** `10.120.4.1:8081`
   - **USB:** `localhost:8081`
4. กด **"Reload"** หรือปิดแอพแล้วเปิดใหม่

---

### วิธีที่ 2: ตรวจสอบว่า Metro Bundler รันอยู่

```bash
# ตรวจสอบ
netstat -ano | findstr :8081
```

**ควรเห็น:** `LISTENING` ที่ port 8081

**ถ้าไม่เห็น:** รัน `npm start`

---

### วิธีที่ 3: ถ้า APK เป็น Release (ไม่มี Dev Menu)

**Release APK ไม่มี Dev Menu** → ต้องใช้วิธีอื่น:

1. **ตรวจสอบว่า Metro Bundler รันอยู่**
2. **กดปุ่ม "RELOAD (R, R)"** ในแอพ
3. **ตรวจสอบ WiFi/USB:** แท็บเล็ตและคอมพิวเตอร์ต้องเชื่อมต่อกัน

---

### วิธีที่ 4: รอ Build ใหม่จาก GitHub Actions

**APK ที่ build อาจยังไม่มี code ที่แก้ไข MainApplication.kt**

1. ตรวจสอบว่า commit ล่าสุดถูก push แล้ว
2. รอให้ GitHub Actions build เสร็จ (ประมาณ 5-10 นาที)
3. ดาวน์โหลด APK ใหม่
4. ติดตั้งใหม่

---

## 🎯 ลำดับการทำ

1. **ตรวจสอบ Metro Bundler:** ต้องรันอยู่
2. **ลองกด RELOAD:** ในแอพ
3. **ตั้งค่า Debug Server Host:** ถ้าเป็น Debug APK
4. **ตรวจสอบ WiFi/USB:** ต้องเชื่อมต่อกัน

---

## 📊 สถานะปัจจุบัน

- ✅ **Metro Bundler:** กำลังรันอยู่
- ✅ **IP Address:** `10.120.4.1` (WiFi) หรือ `192.168.137.1` (USB)
- ⚠️ **แอพ:** ยังไม่เชื่อมต่อ

---

**ลองเขย่าแท็บเล็ต → ตั้งค่า Debug Server Host → Reload แล้วบอกผลด้วย!** 🚀

