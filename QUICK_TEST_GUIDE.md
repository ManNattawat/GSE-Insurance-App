# 🚀 คู่มือทดสอบแบบเร็ว - ไม่ต้อง Build APK

## ⚡ วิธีทดสอบการแก้ไขโค้ดแบบเร็ว (Development Mode)

### 📌 หลักการ:
- **Build APK ครั้งเดียว** → ติดตั้งบนแท็บเล็ต
- **รัน Metro Bundler** → แอพจะเชื่อมต่อกับคอมพิวเตอร์
- **แก้ไขโค้ด** → แอพจะอัปเดตอัตโนมัติ (Hot Reload) **ไม่ต้อง build APK ใหม่!**

---

## 🎯 ขั้นตอนการใช้งาน

### 1️⃣ ครั้งแรก: Build และติดตั้ง Debug APK

```bash
# Terminal 1: เริ่ม Metro Bundler
npm start

# Terminal 2: Build และติดตั้ง APK (ทำครั้งเดียว)
npm run android
```

**รอให้ build เสร็จและติดตั้งบนแท็บเล็ต** (ประมาณ 5-10 นาที)

---

### 2️⃣ ทุกครั้งที่ต้องการทดสอบ: รัน Metro Bundler เท่านั้น!

```bash
# เปิด Terminal 1 ตัวเดียว
npm start
```

**แอพบนแท็บเล็ตจะเชื่อมต่ออัตโนมัติ** (ถ้าเปิดแอพอยู่)

---

## 🔄 การทดสอบการแก้ไข

### หลังจากแก้ไขโค้ด:

1. **บันทึกไฟล์** (Ctrl+S)
2. **แอพจะอัปเดตอัตโนมัติ** (Hot Reload)
   - หรือกด `r` ใน Metro Bundler terminal เพื่อ reload
   - หรือเขย่าแท็บเล็ต → กด "Reload"

### ตัวอย่าง:
- แก้ไข `AutocompleteInput.tsx` → บันทึก → แอพอัปเดตทันที! ⚡
- ไม่ต้อง build APK ใหม่! ✅

---

## 🛠️ คำสั่งที่มีประโยชน์

### ใน Metro Bundler Terminal:
- `r` - Reload app
- `d` - Open Dev Menu
- `j` - Open DevTools
- `Ctrl+C` - Stop Metro Bundler

### บนแท็บเล็ต:
- **เขย่าแท็บเล็ต** → Dev Menu → Reload

---

## ⚠️ เมื่อไหร่ที่ต้อง Build APK ใหม่?

**ต้อง build APK ใหม่เมื่อ:**
- ✅ เปลี่ยน Native Code (Java/Kotlin)
- ✅ เปลี่ยน AndroidManifest.xml
- ✅ เปลี่ยน dependencies ที่มี native modules
- ✅ เปลี่ยน permissions

**ไม่ต้อง build APK ใหม่เมื่อ:**
- ✅ แก้ไข JavaScript/TypeScript
- ✅ แก้ไข UI Components
- ✅ แก้ไข Styles
- ✅ แก้ไข Business Logic

---

## 🎯 สำหรับการแก้ไข AutocompleteInput

1. **แก้ไขโค้ด** → บันทึก
2. **รัน `npm start`** (ถ้ายังไม่รัน)
3. **เปิดแอพบนแท็บเล็ต** → จะอัปเดตอัตโนมัติ
4. **ทดสอบเลือกตัวเลือก** → ควรจะทำงานได้แล้ว!

---

## 💡 Tips

- **เปิดแอพไว้บนแท็บเล็ต** → จะ auto-reload เมื่อแก้ไขโค้ด
- **ใช้ WiFi เดียวกัน** → แอพจะเชื่อมต่อได้ดีกว่า
- **ถ้าไม่เชื่อมต่อ** → เขย่าแท็บเล็ต → Dev Settings → Debug server host → ใส่ IP ของคอมพิวเตอร์

---

**ตอนนี้แก้ไข AutocompleteInput แล้ว - ลองรัน `npm start` และทดสอบดูได้เลย!** 🎉

