# คู่มือการตั้งค่า GSE Insurance App

## 📋 ขั้นตอนการตั้งค่า

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่า Supabase

#### 2.1 สร้าง Supabase Project
1. ไปที่ [Supabase](https://supabase.com)
2. สร้าง Account (ถ้ายังไม่มี)
3. สร้าง Project ใหม่
4. รอให้ Project สร้างเสร็จ (ประมาณ 2-3 นาที)

#### 2.2 สร้าง Table และ View
1. ไปที่ **SQL Editor** ใน Supabase Dashboard
2. เปิดไฟล์ `insurance-schema-setup.sql`
3. Copy โค้ดทั้งหมด
4. วางใน SQL Editor
5. คลิก **Run** เพื่อสร้างตารางและ View
   - ตาราง `insurance.customers` จะถูกสร้างใน schema.insurance
   - View `public.customers` จะถูกสร้างใน schema.public (ชี้ไปที่ insurance.customers)

#### 2.3 ตั้งค่า API Keys
1. ไปที่ **Settings** > **API**
2. Copy **Project URL** และ **anon public key**
3. เปิดไฟล์ `src/config/supabase.ts`
4. แทนที่:
   ```typescript
   const SUPABASE_URL = 'YOUR_SUPABASE_URL';
   const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
   ```
   ด้วย URL และ Key ที่ copy มา

### 3. ทดสอบแอป

#### รันในเบราว์เซอร์ (Web)
```bash
npm run web
```

#### รันบน Android Tablet
```bash
npm run android
```

#### รันบน Expo Go
```bash
npm start
```
แล้วสแกน QR Code ด้วย Expo Go app

## 📱 ฟีเจอร์ของแอป

- ✅ เพิ่มข้อมูลลูกค้า
- ✅ แก้ไขข้อมูลลูกค้า
- ✅ ลบข้อมูลลูกค้า
- ✅ แสดงรายชื่อลูกค้า
- ✅ ส่งออกข้อมูลเป็น Excel
- ✅ เก็บข้อมูลบน Supabase (Cloud)

## 🔧 การใช้งาน

1. **เพิ่มลูกค้า**: คลิกปุ่ม + ที่มุมขวาบน
2. **แก้ไขลูกค้า**: คลิกที่รายการลูกค้า
3. **ลบลูกค้า**: คลิกไอคอนถังขยะ
4. **ส่งออก Excel**: คลิกปุ่ม "ส่งออก Excel"

## 📝 หมายเหตุ

- ข้อมูลจะถูกเก็บไว้บน Supabase Cloud
- ต้องเชื่อมต่อ Internet เพื่อใช้งาน
- Export Excel จะเปิดแชร์ไฟล์ให้ดาวน์โหลด

## 🐛 แก้ไขปัญหา

### แอปไม่เชื่อมต่อ Supabase
- ตรวจสอบว่าใส่ URL และ Key ถูกต้องใน `src/config/supabase.ts`
- ตรวจสอบว่า Table `insurance.customers` และ View `public.customers` ถูกสร้างแล้วใน Supabase
- ตรวจสอบว่า Grant permissions ถูกตั้งค่าแล้ว

### Export Excel ไม่ทำงาน
- ตรวจสอบว่าแอปมีสิทธิ์เข้าถึงไฟล์ระบบ
- บน Android อาจต้องให้สิทธิ์ Storage

