# 🎯 วิธีแก้ไข - APK ใหม่แต่ยังไม่เชื่อมต่อ

## ⚠️ ปัญหา
APK ใหม่ที่ติดตั้งแล้ว แต่ยังเห็น error "Unable to load script"

## 🔍 สาเหตุ
APK ที่ build จาก GitHub Actions อาจเป็น **Release APK** ที่ไม่มี bundle รวมอยู่ และต้องเชื่อมต่อกับ Metro Bundler

---

## ✅ วิธีแก้ไข (ทำตามลำดับ)

### วิธีที่ 1: ตั้งค่า Debug Server Host

**ถ้าเป็น Debug APK (เขย่าแท็บเล็ตแล้วมี Dev Menu):**

1. **เขย่าแท็บเล็ต** → Dev Menu เปิดขึ้นมา
2. กด **"Settings"** หรือ **"Debug server host & port for device"**
3. ใส่:
   - **WiFi:** `10.120.4.1:8081`
   - **USB:** `localhost:8081`
4. กด **"Reload"**

---

### วิธีที่ 2: ถ้าเป็น Release APK (เขย่าแล้วไม่มี Dev Menu)

**Release APK ไม่มี Dev Menu** → ต้องใช้วิธีนี้:

1. **ตรวจสอบ Metro Bundler:**
   - ต้องรันอยู่ (ฉันตรวจสอบแล้ว ✅)
   
2. **ตรวจสอบการเชื่อมต่อ:**
   - แท็บเล็ตและคอมพิวเตอร์ต้องอยู่ใน **WiFi เดียวกัน**
   - หรือเชื่อมต่อผ่าน **USB**

3. **กดปุ่ม "RELOAD (R, R)"** ในแอพอีกครั้ง

4. **ปิดแอพแล้วเปิดใหม่**

---

### วิธีที่ 3: รอ Build APK ใหม่ (ถ้ายังไม่ได้)

**APK ที่ build อาจยังไม่มี code ที่แก้ไข MainApplication.kt**

1. ตรวจสอบ GitHub Actions:
   - ไปที่: `https://github.com/ManNattawat/GSE-Insurance-App/actions`
   - ดู workflow ล่าสุด → ต้อง build หลังจาก commit `c4f7437`
   
2. ถ้ายัง build เก่า → รอให้ build ใหม่เสร็จ

3. ดาวน์โหลด APK ใหม่ → ติดตั้ง

---

## 🎯 ลำดับการทำตอนนี้

1. **ลองเขย่าแท็บเล็ต** → ดูว่ามี Dev Menu หรือไม่
2. **ถ้ามี Dev Menu:**
   - ตั้งค่า Debug Server Host = `10.120.4.1:8081` (WiFi) หรือ `localhost:8081` (USB)
   - Reload
3. **ถ้าไม่มี Dev Menu:**
   - ปิดแอพ → เปิดใหม่
   - กด RELOAD อีกครั้ง
   - ตรวจสอบ WiFi/USB

---

## 📊 สถานะ

- ✅ Metro Bundler: รันอยู่
- ✅ Code: แก้ไขแล้ว (MainApplication.kt)
- ⚠️ APK: อาจเป็นตัวเก่าหรือ Release

---

**ลองเขย่าแท็บเล็ตก่อน → บอกว่ามี Dev Menu หรือไม่** 🚀

