# วิธีทดสอบก่อน Build APK

## 🎯 วิธีทดสอบ (เรียงตามความง่าย)

### 1. **Web Browser** ✅ (ง่ายที่สุด)
```bash
npm run web
```
- ✅ เร็ว ไม่ต้อง build
- ✅ ทดสอบ UI และ logic ได้
- ⚠️ บางฟีเจอร์ไม่ทำงาน (File System, Sharing)

---

### 2. **Android Emulator** (ถ้ามี Android Studio)
```bash
npm run android
```
- ✅ ทดสอบได้ครบทุกฟีเจอร์
- ⚠️ ต้องติดตั้ง Android Studio ก่อน
- ⚠️ ใช้ RAM มาก

---

### 3. **Development Build** (Expo)
```bash
eas build --platform android --profile development
```
- ✅ รองรับ dependencies ทั้งหมด
- ✅ Hot reload ได้
- ⚠️ ต้อง build ก่อน (ใช้เวลา)

---

### 4. **Expo Go** ❌ (ไม่รองรับ)
- ❌ ไม่รองรับ `@react-navigation/native`
- ❌ ไม่รองรับ `xlsx`
- ❌ ต้องลบ dependencies ออก (ไม่แนะนำ)

---

## 🔧 ถ้าต้องการใช้ Expo Go (ไม่แนะนำ)

### ต้องลบ:
```json
"@react-navigation/native"
"@react-navigation/bottom-tabs"
"@react-navigation/native-stack"
"react-native-screens"
"react-native-safe-area-context"
"xlsx"
```

### ใช้แทน:
- Expo Router (แทน React Navigation)
- ใช้ Expo APIs แทน native modules

**⚠️ ไม่แนะนำ:** จะต้องเขียนโค้ดใหม่เยอะมาก

---

## ✅ แนะนำ

### สำหรับทดสอบตอนนี้:
**ใช้ Web Browser** (เร็วที่สุด)
```bash
npm run web
```

### สำหรับทดสอบบน Tablet:
**Build APK** (แนะนำ)
```bash
eas build --platform android --profile preview
```

---

## 📝 สรุป

| วิธี | ความเร็ว | รองรับ Dependencies | เหมาะกับ |
|------|---------|-------------------|---------|
| Web Browser | ⚡⚡⚡ | ✅ | ทดสอบ UI |
| Android Emulator | ⚡⚡ | ✅ | ทดสอบครบ |
| Development Build | ⚡ | ✅ | Development |
| Expo Go | ⚡⚡⚡ | ❌ | ไม่แนะนำ |

