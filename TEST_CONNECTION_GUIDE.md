# 🧪 คู่มือทดสอบการเชื่อมต่อแท็บเล็ต

## ✅ สถานะปัจจุบัน

### 1. Metro Bundler
- ✅ **กำลังรันอยู่** (port 8081)
- ✅ พร้อมให้แอพเชื่อมต่อ

### 2. React Native Environment
- ✅ Node.js, npm, Metro - พร้อม
- ✅ Android Studio, JDK, Gradle - พร้อม
- ✅ adb - พร้อม (ผ่าน React Native CLI)

---

## 🎯 สิ่งที่ทำได้ตอนนี้

### 1. **Hot Reload** ⚡
- แก้ไขโค้ด → บันทึกไฟล์ → แอพอัปเดตอัตโนมัติ
- **วิธีใช้:** 
  - แก้ไขไฟล์ → บันทึก (Ctrl+S)
  - แอพบนแท็บเล็ตจะอัปเดตทันที

### 2. **ดู Log จากแท็บเล็ต** 📋
- **คำสั่ง:** `npx react-native log-android`
- **แสดง:** Error, Warning, Console.log จากแอพ
- **หยุด:** กด Ctrl+C

### 3. **Reload แอพ** 🔄
- **วิธีที่ 1:** เขย่าแท็บเล็ต → Reload
- **วิธีที่ 2:** กด `r` ใน Metro Bundler terminal
- **วิธีที่ 3:** กดปุ่ม "RELOAD (R, R)" ในแอพ

### 4. **เปิด Dev Menu** 🛠️
- **วิธีที่ 1:** เขย่าแท็บเล็ต (ถ้าเป็น Debug APK)
- **วิธีที่ 2:** ใช้ adb command (ถ้ามี adb)
- **วิธีที่ 3:** กดปุ่ม Back 3 ครั้ง (บางครั้งใช้ได้)

### 5. **Port Forwarding (USB)** 🔌
- React Native CLI จะทำอัตโนมัติเมื่อรัน `npm run android`
- หรือใช้: `adb reverse tcp:8081 tcp:8081`

---

## 🧪 ทดสอบตอนนี้

### ทดสอบ 1: Hot Reload
1. เปิดแอพบนแท็บเล็ต
2. แก้ไขสีพื้นหลังใน `screens/HomeScreen.tsx`:
   ```typescript
   backgroundColor: '#FFE0B2', // เปลี่ยนเป็นสีส้มอ่อน
   ```
2. บันทึกไฟล์ (Ctrl+S)
3. **ดูที่แท็บเล็ต** → ควรเห็นสีเปลี่ยนทันที! ⚡

### ทดสอบ 2: ดู Log
1. รัน: `npx react-native log-android`
2. ใช้งานแอพบนแท็บเล็ต
3. **ดู log ใน terminal** → เห็น error/warning/log

### ทดสอบ 3: Reload
1. กด `r` ใน Metro Bundler terminal
2. **ดูที่แท็บเล็ต** → แอพจะ reload

---

## 📊 สรุป

| ฟีเจอร์ | สถานะ | วิธีใช้ |
|---------|-------|---------|
| Hot Reload | ✅ พร้อม | แก้ไขโค้ด → บันทึก |
| ดู Log | ✅ พร้อม | `npx react-native log-android` |
| Reload | ✅ พร้อม | กด `r` หรือเขย่าแท็บเล็ต |
| Dev Menu | ⚠️ ต้องเป็น Debug APK | เขย่าแท็บเล็ต |
| Port Forwarding | ✅ อัตโนมัติ | เมื่อรัน `npm run android` |

---

## 💡 Tips

1. **ถ้าแอพไม่อัปเดต:**
   - ตรวจสอบว่า Metro Bundler รันอยู่
   - Reload แอพ (กด `r`)
   - ตรวจสอบ WiFi (ถ้าใช้ WiFi)

2. **ถ้าไม่เห็น Dev Menu:**
   - ต้องติดตั้ง Debug APK (ไม่ใช่ Release)
   - หรือใช้ adb command

3. **ถ้าแอพ crash:**
   - ดู log: `npx react-native log-android`
   - ตรวจสอบ error ใน Metro Bundler terminal

---

**พร้อมทดสอบแล้ว! ลองแก้ไขโค้ดแล้วดูว่า Hot Reload ทำงานหรือไม่** 🚀

