# แก้ไขปัญหา - Expo Go Error

## ❌ Error: "Failed to download remote update"

### สาเหตุ:
แอปใช้ dependencies ที่ Expo Go ไม่รองรับ:
- `@react-navigation/native`
- `xlsx` (อาจมี native modules)
- Dependencies อื่นๆ ที่ต้องการ native code

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: รันใน Web Browser (เร็วที่สุด - สำหรับทดสอบ)

```bash
npm run web
```

**ข้อดี:**
- ✅ รวดเร็ว ไม่ต้อง build
- ✅ เหมาะสำหรับทดสอบ UI และฟังก์ชันพื้นฐาน
- ✅ ไม่มีปัญหา dependencies

**ข้อจำกัด:**
- ⚠️ บางฟีเจอร์อาจไม่ทำงาน (เช่น File System, Sharing)

---

### วิธีที่ 2: Build APK (แนะนำสำหรับ Tablet)

#### ขั้นตอน:

1. **ติดตั้ง EAS CLI:**
   ```bash
   npm install -g eas-cli
   ```

2. **Login:**
   ```bash
   eas login
   ```

3. **Build APK:**
   ```bash
   eas build --platform android --profile preview
   ```

4. **Download APK:**
   - รอ build เสร็จ (ประมาณ 10-20 นาที)
   - Download APK จาก Expo Dashboard
   - ติดตั้งบน Tablet

**ข้อดี:**
- ✅ ทำงานได้ครบทุกฟีเจอร์
- ✅ เหมาะสำหรับ Tablet
- ✅ ไม่ต้องใช้ Expo Go

---

### วิธีที่ 3: ใช้ Development Build (สำหรับ Development)

1. **สร้าง Development Build:**
   ```bash
   eas build --platform android --profile development
   ```

2. **ติดตั้ง Development Build บน Tablet**

3. **รันแอป:**
   ```bash
   npm start
   ```
   แล้วสแกน QR Code ด้วย Development Build

---

## 🎯 แนะนำ

### สำหรับการทดสอบตอนนี้:
**ใช้วิธีที่ 1: รันใน Web Browser**
```bash
npm run web
```

### สำหรับใช้งานจริงบน Tablet:
**ใช้วิธีที่ 2: Build APK**
```bash
eas build --platform android --profile preview
```

---

## 📝 หมายเหตุ

- Expo Go มีข้อจำกัด: รองรับเฉพาะ Expo SDK packages เท่านั้น
- Dependencies ที่ใช้ native code ต้องใช้ Development Build หรือ APK
- สำหรับแอปนี้ ควร Build APK เพื่อใช้งานจริง

