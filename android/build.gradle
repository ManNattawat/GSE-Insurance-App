// Top-level build file where you can add configuration options common to all sub-projects/modules.
// Self-reliant: ระบุ version ทั้งหมดเอง ไม่พึ่ง plugin จัดการให้

buildscript {
  ext {
    buildToolsVersion = "35.0.0"
    minSdkVersion = 24
    compileSdkVersion = 35
    targetSdkVersion = 34
    kotlinVersion = "2.0.21"
    ndkVersion = "26.1.10909125"
  }
  
  // Resolve react-native-gradle-plugin jar file จาก node_modules โดยตรง
  // แอพนี้เป็นแอพกรอกข้อมูลง่ายๆ - ใช้ jar file โดยตรงเพื่อลดความซับซ้อน
  // ไม่ใช้ includeBuild หรือ settings plugin - ตรงไปตรงมา
  def reactNativeGradlePluginJar = new File(
    new File(
      providers.exec {
        workingDir(rootDir)
        commandLine("node", "--print", "require.resolve('@react-native/gradle-plugin/package.json', { paths: [require.resolve('react-native/package.json')] })")
      }.standardOutput.asText.get().trim()
    ).getParentFile(),
    "react-native-gradle-plugin/build/libs/react-native-gradle-plugin.jar"
  )
  
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    // Self-reliant: ระบุ version ของ Android Gradle Plugin เอง
    classpath("com.android.tools.build:gradle:8.7.3")
    // react-native-gradle-plugin: ใช้ jar file โดยตรงใน buildscript
    // แอพนี้เป็นแอพกรอกข้อมูลง่ายๆ - ใช้ jar file โดยตรงเพื่อลดความซับซ้อน
    classpath(files(reactNativeGradlePluginJar))
    classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion")
  }
}

def reactNativeAndroidDir = new File(
  providers.exec {
    workingDir(rootDir)
    commandLine("node", "--print", "require.resolve('react-native/package.json')")
  }.standardOutput.asText.get().trim(),
  "../android"
)

allprojects {
  repositories {
    maven {
      // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
      url(reactNativeAndroidDir)
    }

    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
  }
}

// Self-reliant: ลบ rootproject plugin ออก - แอพนี้เป็นแอพกรอกข้อมูลง่ายๆ ไม่จำเป็นต้องใช้
// (app/build.gradle ใช้ com.facebook.react plugin แล้ว ซึ่งเพียงพอ)
// apply plugin: "com.facebook.react.rootproject" // ลบออกเพื่อลดความซับซ้อน
