// Self-reliant: แอพนี้เป็นแอพกรอกข้อมูลง่ายๆ - ลดความซับซ้อน
// ไม่ใช้ includeBuild หรือ settings plugin - ใช้ jar file โดยตรงใน build.gradle
// ลดการพึ่งพาและควบคุมได้ง่ายขึ้น

rootProject.name = 'GSE Insurance App'

include ':app'
