# 🚀 วิธีรันแอพบนแท็บเล็ตโดยไม่ต้อง Build APK

## 🎯 วิธีที่ 1: ใช้ Expo Go (เร็วที่สุด - แนะนำ!)

**ข้อดี:**
- ✅ ไม่ต้อง build APK เลย
- ✅ รันได้ทันที (ไม่กี่วินาที)
- ✅ ไม่ใช้ทรัพยากรเครื่อง
- ✅ Hot Reload - แก้โค้ดแล้วเห็นผลทันที

**ข้อเสีย:**
- ⚠️ บาง native modules อาจใช้ไม่ได้ (แต่ส่วนใหญ่ใช้ได้)

### ขั้นตอน:

#### 1. ติดตั้ง Expo Go บนแท็บเล็ต
- ไปที่ Play Store
- ค้นหา "Expo Go"
- ติดตั้ง

#### 2. รัน Expo Server
```bash
cd D:\projects\GSE-Insurance-App
npm start
```

#### 3. เชื่อมต่อแท็บเล็ต
- **วิธีที่ 1:** สแกน QR code ด้วย Expo Go
- **วิธีที่ 2:** เปิด Expo Go → ใส่ URL ที่แสดงใน terminal

#### 4. แอพจะเปิดขึ้นมาเอง!

---

## 🎯 วิธีที่ 2: ใช้ GitHub Actions (Build บน Cloud)

**ข้อดี:**
- ✅ ไม่ใช้ทรัพยากรเครื่องเลย
- ✅ Build บน cloud (เร็ว)
- ✅ ได้ APK จริง

**วิธีใช้:**
1. Push code ขึ้น GitHub
2. GitHub Actions จะ build APK อัตโนมัติ
3. ดาวน์โหลด APK จาก Actions → Artifacts

---

## 🎯 วิธีที่ 3: ใช้ EAS Build (Cloud Build)

**ข้อดี:**
- ✅ Build บน cloud
- ✅ ไม่ใช้ทรัพยากรเครื่อง

**วิธีใช้:**
```bash
npm install -g eas-cli
eas login
eas build --platform android --profile preview
```

---

## 📊 เปรียบเทียบ

| วิธี | ใช้ทรัพยากรเครื่อง | ความเร็ว | เหมาะกับ |
|------|------------------|---------|---------|
| **Expo Go** | ❌ ไม่ใช้เลย | ⚡⚡⚡ เร็วมาก | ทดสอบ UI, ฟีเจอร์ |
| **GitHub Actions** | ❌ ไม่ใช้เลย | ⚡⚡ เร็ว | Build APK จริง |
| **EAS Build** | ❌ ไม่ใช้เลย | ⚡⚡ เร็ว | Build APK จริง |
| **Local Build** | ⚠️ ใช้มาก | ⚡ ช้า | เมื่อต้องการ build เอง |

---

## ✅ คำแนะนำ

**สำหรับโน๊ตบุ๊ค:**
1. **ทดสอบ:** ใช้ Expo Go (เร็ว ไม่ใช้ทรัพยากร)
2. **Build APK:** ใช้ GitHub Actions (push code แล้วได้ APK)

**ไม่ต้อง build บนเครื่องเลย!**

