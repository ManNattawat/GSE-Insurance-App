# 📱 วิธีรันแอพบนแท็บเล็ตเพื่อทดสอบก่อน Build

## 🎯 วิธีที่ 1: ใช้ Expo Development Build (แนะนำ)

**ข้อดี:**
- ✅ รันได้ทันที ไม่ต้อง build APK
- ✅ Hot Reload - แก้โค้ดแล้วเห็นผลทันที
- ✅ เร็วมาก - ไม่ต้องรอ build

### ขั้นตอน:

#### 1. เสียบ USB และเปิด USB Debugging
- Settings → About tablet → กด "Build number" 7 ครั้ง
- Settings → Developer options → เปิด "USB debugging"

#### 2. ตรวจสอบว่าแท็บเล็ตเชื่อมต่อแล้ว
```bash
cd D:\projects\GSE-Insurance-App
adb devices
```

**ผลลัพธ์ที่คาดหวัง:**
```
List of devices attached
XXXXXXXX    device
```

#### 3. รันแอพบนแท็บเล็ต
```bash
cd D:\projects\GSE-Insurance-App
npm start
```

**หรือ:**
```bash
npx expo start --localhost --android
```

#### 4. เลือกตัวเลือก
- กด `a` เพื่อรันบน Android
- หรือสแกน QR code ด้วย Expo Go (ถ้ามี)

---

## 🎯 วิธีที่ 2: ใช้ Expo Go (ถ้าใช้ได้)

**ข้อดี:**
- ✅ ไม่ต้อง build เลย
- ✅ รันได้ทันที

**ข้อเสีย:**
- ⚠️ บาง native modules อาจใช้ไม่ได้

### ขั้นตอน:

1. ติดตั้ง Expo Go จาก Play Store บนแท็บเล็ต
2. รัน `npm start`
3. สแกน QR code ด้วย Expo Go

---

## 🎯 วิธีที่ 3: Build Development APK (ถ้าวิธีอื่นไม่ได้)

**ข้อดี:**
- ✅ ใช้ native modules ได้ครบ
- ✅ เหมือน production build

### ขั้นตอน:

#### 1. Prebuild Android project
```bash
cd D:\projects\GSE-Insurance-App
npx expo prebuild --platform android
```

#### 2. Build Debug APK
```bash
cd android
.\gradlew.bat assembleDebug
```

#### 3. ติดตั้งบนแท็บเล็ต
```bash
.\gradlew.bat installDebug
```

**หรือ:**
- Copy APK จาก `android/app/build/outputs/apk/debug/app-debug.apk`
- ส่งไปแท็บเล็ตและติดตั้ง

---

## 🔧 Troubleshooting

### ปัญหา: `adb devices` ไม่เจอแท็บเล็ต
**แก้ไข:**
1. ตรวจสอบ USB cable
2. ตรวจสอบ USB drivers
3. ลองเปลี่ยน USB port
4. เปิด USB debugging อีกครั้ง

### ปัญหา: Expo ไม่เจอ device
**แก้ไข:**
```bash
# ตรวจสอบ devices
adb devices

# Restart adb
adb kill-server
adb start-server
```

### ปัญหา: Network error
**แก้ไข:**
- ใช้ `--localhost` flag (ใช้อยู่แล้ว)
- ตรวจสอบว่าแท็บเล็ตและคอมพิวเตอร์อยู่ใน network เดียวกัน
- หรือใช้ USB tethering

---

## 📝 คำสั่งที่ใช้บ่อย

```bash
# เริ่ม Expo server
npm start

# รันบน Android (auto-detect device)
npm run android

# ตรวจสอบ devices
adb devices

# Restart adb
adb kill-server && adb start-server

# ดู logs
adb logcat
```

---

## ✅ Checklist ก่อน Build

- [ ] ทดสอบทุกหน้าจอ
- [ ] ทดสอบบันทึกข้อมูล
- [ ] ทดสอบดึงข้อมูล
- [ ] ทดสอบค้นหา
- [ ] ทดสอบส่งออก CSV
- [ ] ทดสอบอัปโหลดรูป
- [ ] ไม่มี error
- [ ] UI ใช้งานได้ดี

**เมื่อทุกอย่างเรียบร้อย → Build APK!**

